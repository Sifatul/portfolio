const Footer = () => {
    return (<     div class=" row footer " >
        <    p class="m-auto py-2" > Copyright & reg; sifatul @cbnu.ac.kr < /p>
        < /        div > 
        )}
export default Footer